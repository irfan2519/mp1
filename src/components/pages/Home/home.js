import React from "react";
import HeaderMenu from "../Shared/Header/header";

import "./home.scss";

function Home() {
  return (
    <div>
      <HeaderMenu />

      {/* <div>
        <h2>Responsive Search Bar Home page</h2>
        <p>Navigation bar with a search box inside of it.</p>
        <p>Resize the browser window to see the responsive effect.</p>
        <p>
          For more examples on how to add a submit button and icon inside the
          search bar, go back to the tutorial.
        </p>
      </div> */}
    </div>
  );
}

export default Home;
